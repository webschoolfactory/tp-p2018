<% layout( 'layout' ) -%>

<h1 id="page-title"><%= title %></h1>

<div id="list">
<img src="../public/images/giphy.gif">
  <form action="/create" method="post" accept-charset="utf-8">
    <div class="item-new">
      <input class="input" type="text" name="content" />
      <p> Pour régler les conflits github il faut une bonne poule que l'on merge afin de l'add au four. Une fois prête on appelle tout le monde en criant "come eat". Pour finir, on push la poule cuite dans nos assiettes </p>
    </div>
  </form>

<% todos.forEach( function ( todo ){ %>
  <div class="item">
    <a class="update-link" href="/edit/<%= todo._id %>" title="Update this todo item"><%= todo.content %></a>
    <a class="del-btn" href="/destroy/<%= todo._id %>" title="Delete this todo item">Delete</a>
  </div>
<% }); %>
</div>
